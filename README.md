express + next.js + React.js + Material UI sample

commands
```
yarn install
yarn run dev
```

if had not to install mysql your local machine. 
view connection timeout error in mysql by terminal. 

but next.js and express server is not problem, you can viewing sample page.